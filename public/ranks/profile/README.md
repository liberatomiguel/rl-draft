# Rank images — PROFILE art set

Drop one PNG per rank here (shown on the profile screen — a different art
set from the menu). Exact filenames:

```
unranked.png
bronze.png
silver.png
gold.png
platinum.png
diamond.png
champion.png
grand-champion.png
supersonic-legend.png
```

Recommended: square images with transparent background, ≥240×240px.
While a file is missing, the app shows a styled CSS placeholder automatically
(component: `src/components/ui/RankBadge.tsx`, variant `profile`).
